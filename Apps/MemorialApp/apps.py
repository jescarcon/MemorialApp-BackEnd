from django.apps import AppConfig


class MemorialappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Apps.MemorialApp' #Le decimos que la app se llama MemorialApp y que esta dentro de Apps
